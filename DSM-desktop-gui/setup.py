from distutils.core import setup

setup(
    name='DSM-gui',
    version='0.8',
    author='M. Wysokinski',
    description='Desktop GUI for the DSM project',
    install_requires=[
        "twisted == 13.2.0",
    ],
)